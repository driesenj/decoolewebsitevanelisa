// Paste the config of your Firebase web app here (Firebase console -> Project settings -> Your apps -> SDK setup and configuration -> Config).
// Leave `null` and the site runs in offline mode: guestbook shows the seed entries, new entries stay on the device.
// The apiKey is not a secret: access is controlled by firebase/firestore.rules.
export const firebaseConfig = null;

/* Example:
export const firebaseConfig = {
  apiKey: "AIza....",
  authDomain: "elisa-fanclub.firebaseapp.com",
  projectId: "elisa-fanclub",
  storageBucket: "elisa-fanclub.appspot.com",
  messagingSenderId: "1234567890",
  appId: "1:1234567890:web:abcdef"
};
*/
